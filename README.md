> Empty project with basic configs.
> Configuration files contains basic Gulp file.
> Its contains webpack, scss, sprite generator & live reload all changes with browse-sync.
>
> To start project do: npm install
> then
> gulp